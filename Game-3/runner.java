
/**
 * Write a description of class runner here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class runner
{
    
    /*private static boolean checkInput =true;
    private static Scanner scanner = new Scanner(System.in);
    public static void main(String[] args) throws InterruptedException {
        
        
        
        sizeSituation run = new sizeSituation();
        backgroundUpdate background = new backgroundUpdate();
        Thread backgroundThread = new Thread(new backgroundUpdate());
        travelUpdate traveler = new travelUpdate(1,1000);
        
        backgroundThread.start();
        
        
        Scanner scanner = new Scanner(System.in);
        
        
        while(checkInput){
            String input = scanner.next();
            if (input.equalsIgnoreCase("stop")) {
                backgroundThread.suspend();
                run.options();
                backgroundThread.resume();
            }
            else if (input.equalsIgnoreCase("terminate")){
                System.out.println("Journey end");
                System.out.println("Input any character to stop program");
                background.stop();
                checkInput=false;
                backgroundThread.interrupt();
            }
        }
    }
    public void stopCheckInput(){
        scanner.close();
        checkInput=false;
        
    }*/
}

